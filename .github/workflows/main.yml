name: RDP via Tailscale (Final Stable Version)
on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 180

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 1️⃣ Aktifkan RDP dan firewall
      - name: Enable Remote Desktop
        shell: pwsh
        run: |
          Write-Host "🔧 Mengaktifkan RDP dan membuka firewall..."
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Restart-Service -Name TermService -Force
          Write-Host "✅ Remote Desktop sudah aktif."

      # 2️⃣ Buat user dengan password aman (≤14 karakter, ASCII khusus dihindari)
      - name: Create Local Admin User
        id: user
        shell: pwsh
        run: |
          $password = -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 12 | ForEach-Object {[char]$_})
          net user RDP $password /add /Y
          net localgroup administrators RDP /add
          Add-Content $env:GITHUB_ENV "RDP_PASSWORD=$password"
          Write-Host "✅ User RDP dibuat: RDP / $password"

      # 3️⃣ Instal Tailscale menggunakan EXE (hindari MSI error)
      - name: Install Tailscale Client
        shell: pwsh
        run: |
          Write-Host "📦 Menginstal Tailscale..."
          $exe = "$env:TEMP\tailscale-setup.exe"
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe" -OutFile $exe
          Start-Process $exe -ArgumentList "/quiet /norestart" -Wait
          Write-Host "✅ Instalasi Tailscale selesai."

      # 4️⃣ Hubungkan runner ke jaringan Tailscale
      - name: Connect to Tailscale Network
        shell: pwsh
        run: |
          $ts = "C:\Program Files\Tailscale\tailscale.exe"
          $authkey = "${{ secrets.TS_AUTH_KEY }}"
          if ([string]::IsNullOrEmpty($authkey)) {
            Write-Host "⚠️ Auth Key tidak ditemukan! Jalankan login manual..."
            & $ts up --hostname=gh-runner-${{ github.run_id }} --accept-dns=true --accept-routes=true
          } else {
            Write-Host "🔐 Menghubungkan node ke Tailnet..."
            & $ts up --auth-key=$authkey --hostname=gh-runner-${{ github.run_id }} --accept-dns=true --accept-routes=true
          }
          if ($LASTEXITCODE -ne 0) { throw "❌ Gagal connect ke Tailnet." }
          Write-Host "✅ Node tersambung ke Tailnet."

      # 5️⃣ Tampilkan IP Tailscale dan kredensial akses RDP
      - name: Display Access Information
        shell: pwsh
        run: |
          $ts = "C:\Program Files\Tailscale\tailscale.exe"
          $ip = & $ts ip -4
          Write-Host "`n==============================="
          Write-Host "✅ Tailscale dan RDP Siap Digunakan"
          Write-Host "🌐 IP Tailscale: $ip"
          Write-Host "👤 Username: RDP"
          Write-Host "🔑 Password: $env:RDP_PASSWORD"
          Write-Host "Gunakan aplikasi mstsc.exe dengan IP di atas."
          Write-Host "==============================="

     - name: Keep Session Alive (6 hours)
  shell: pwsh
  run: |
    Write-Host "Menjaga RDP aktif selama 6 jam..."
    for ($i=0; $i -lt 360; $i++) {
      Write-Host "[$(Get-Date -Format 'HH:mm:ss')] RDP aktif..."
      Start-Sleep -Seconds 60
    }

            Write-Host "[$(Get-Date -Format 'HH:mm:ss')] Session RDP aktif..."
            Start-Sleep -Seconds 60
          }
